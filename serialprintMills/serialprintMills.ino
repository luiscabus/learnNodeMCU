unsigned long tempo_inicial, tempo_atual, tempo_futuro;

void setup() {
  // put your setup code here, to run once:
  delay(200);
  Serial.begin(115200);
  tempo_inicial = millis();
  Serial.print("Tempo Inicial: ");
  Serial.println(tempo);
}

void loop() {
  // put your main code here, to run repeatedly:

  
  if tempo_inicial + 
  
  Serial.print("Hora: ");
  Serial.print(tempo);
  Serial.print("\n");
  delay(1000);
  
}
